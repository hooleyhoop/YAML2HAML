.playlist
  - if( !defined?(_) || _.indexed_subrenderers.count==0 )
    .placeholder
      %p
        placeholder content
  - else
    - _.indexed_subrenderers.each_with_index do |subrenderer, i|
      = _.insert(i)
      
  %p
    %a( href='apple.com') see more